[project]
name = "lhf"
version = "0.1.0"
requires-python = ">=3.10"

dependencies = [
    "jax",
    "flax",
    "optax",
    "matplotlib",
]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["lhf*"]